# Обратные задачи нейронных PDE: Реалистичные EEG данные

## 🧠 Новые возможности: Реалистичная генерация EEG данных

Мы добавили мощные возможности для создания реалистичных данных EEG для тестирования и демонстрации алгоритмов решения обратных задач!

### ✨ Что нового:

- **Реалистичная форма головы**: Эллипсоид вместо сферы с разными типами головы
- **Множественные типы головы**: child, adult, large_adult, female, male с реалистичными размерами
- **Асимметричная форма**: Учет анатомических особенностей (затылок, асимметрия полушарий)
- **Реалистичные EEG датчики**: Размещение на эллипсоидальной поверхности головы
- **Множественные источники сигнала**: Моделирование активных областей мозга
- **Временная динамика**: Гауссовские пики с настраиваемыми параметрами
- **Пространственное затухание**: Реалистичное уменьшение сигнала с расстоянием
- **Комплексная визуализация**: 3D/2D графики с размерами головы
- **Легкая интеграция**: Готовые данные можно сразу использовать с решателем

## 🚀 Быстрый старт

### 1. Базовый пример

```python
from src.inverse_npde.eeg_data_generator import create_eeg_experiment

# Создаем реалистичный EEG эксперимент с эллипсоидальной головой
measured_points, generator = create_eeg_experiment(
    num_sensors=32,        # 32 датчика на голове
    num_time_points=15,    # 15 временных отсчетов
    noise_level=0.01,      # 1% шума
    head_type="adult",    # Тип головы: child, adult, female, male, large_adult
    save_visualization=True
)

print(f"Сгенерировано {len(measured_points)} измеренных точек")

# Показываем размеры головы
dimensions = generator.sensor_gen.get_head_dimensions()
print(f"Размеры головы: X={dimensions['rx']:.1f}, Y={dimensions['ry']:.1f}, Z={dimensions['rz']:.1f}")
```

### 2. Использование с решателем

```python
from src.inverse_npde.julia_interface import EEGInverseSolver

# Создаем решатель
solver = EEGInverseSolver(
    neural_config={
        'hidden_layers': [64, 32],
        'activation': 'tanh',
    }
)

# Решаем обратную задачу с реалистичными данными
results = solver.solve(measured_points)

if results['solved']:
    print(f"Эксперимент успешен! MSE: {results['metrics']['mse']:.6f}")
```

### 3. Кастомная конфигурация с разными типами головы

```python
from src.inverse_npde.eeg_data_generator import EEGDataGenerator

# Создаем генератор с женской головой (более округлая)
generator = EEGDataGenerator(
    num_sensors=64,    # Больше датчиков для лучшего покрытия
    num_time_points=20,
    noise_level=0.02,   # Больше шума для реалистичности
    head_type="female"  # Тип головы
)

# Добавляем источники сигнала
generator.add_source(
    position=[-4.0, 3.0, 1.0],  # Левый участок коры
    amplitude=1.2,
    peak_time=0.3,
    sigma_time=0.06
)

generator.add_source(
    position=[4.0, -2.0, 2.0],  # Правый участок коры
    amplitude=0.9,
    peak_time=0.7,
    sigma_time=0.1
)

# Генерируем данные
measured_points = generator.generate_measured_points()
generator.visualize_data(measured_points, "female_head_experiment")

# Сравниваем с мужской головой (более вытянутая)
male_generator = EEGDataGenerator(
    num_sensors=64,
    num_time_points=20,
    noise_level=0.02,
    head_type="male"
)
male_generator.create_default_sources()
male_points = male_generator.generate_measured_points()
```

## 📊 Примеры и демо

### Jupyter ноутбук с полным примером

Запустите ноутбук для интерактивного изучения:

```bash
jupyter notebook examples/eeg_realistic_demo.ipynb
```

Этот ноутбук включает:
- Генерацию реалистичных EEG данных
- Решение обратной задачи
- Визуализацию результатов
- Сравнение с аналитическим решением
- Анализ качества восстановления

### Командная строка

Используйте демо скрипт для быстрых тестов:

```bash
# Базовый эксперимент
python examples/eeg_data_demo.py basic

# Демонстрация разных типов головы
python examples/eeg_data_demo.py head_types

# Кастомный эксперимент с множественными источниками
python examples/eeg_data_demo.py custom

# Сравнение разных конфигураций
python examples/eeg_data_demo.py comparison

# Анализ сохраненных данных
python examples/eeg_data_demo.py analyze demo_eeg_data.json
```

## 👤 Типы головы и их особенности

### Доступные типы головы:

| Тип | Описание | Размеры (X, Y, Z) | Особенности |
|-----|----------|-------------------|-------------|
| `child` | Детская голова | 7.0, 8.0, 9.0 | Меньше размеры, пропорционально другая форма |
| `adult` | Стандартная взрослая | 8.5, 9.5, 10.5 | Базовая форма, сбалансированные пропорции |
| `large_adult` | Крупная взрослая | 9.5, 10.5, 11.5 | Увеличенные размеры |
| `female` | Женская голова | 8.0, 9.0, 10.0 | Более округлая форма |
| `male` | Мужская голова | 9.0, 10.0, 11.0 | Более вытянутая форма |

### Анатомические особенности:

- **Асимметрия полушарий**: Левая сторона немного меньше правой
- **Выступающий затылок**: Задняя часть головы увеличена на 10%
- **Эллипсоидальная форма**: Реалистичные пропорции вместо идеальной сферы

### Пример использования разных типов:

```python
# Сравнение детской и взрослой головы
for head_type in ['child', 'adult']:
    measured_points, generator = create_eeg_experiment(
        num_sensors=32,
        num_time_points=10,
        head_type=head_type,
        save_visualization=True
    )
    
    dimensions = generator.sensor_gen.get_head_dimensions()
    print(f"Голова {head_type}: {dimensions['rx']:.1f} x {dimensions['ry']:.1f} x {dimensions['rz']:.1f}")
```

## � Архитектурные изменения

### Обновленный интерфейс решателя

Решатель теперь использует чистую архитектуру без measured_points в конструкторе:

```python
# ✅ Правильно (новый интерфейс)
solver = EEGInverseSolver()
results = solver.solve(measured_points)

# ❌ Неправильно (старый интерфейс)
solver = EEGInverseSolver(measured_points=measured_points)
```

### Структура данных

Каждая измеренная точка:
```python
[x, y, z, t, measurement]
```

Где:
- `x, y, z` - координаты датчика на сфере
- `t` - время измерения
- `measurement` - значение сигнала

## 📈 Результаты и визуализация

### Автоматическая визуализация

При генерации данных создаются:
1. **3D расположение датчиков** на сферической поверхности
2. **2D проекция** для анализа покрытия
3. **Временные ряды** для отдельных датчиков
4. **Пространственное распределение** в разные моменты времени

### Выходные данные

- **JSON файлы** с полной информацией об эксперименте
- **PNG графики** высокого разрешения
- **Статистика** качества данных
- **Параметры источников** для валидации

## 🎯 Практические рекомендации

### Количество датчиков

- **16-32**: Быстрые эксперименты и демо
- **64**: Стандартные исследования
- **128+**: Высокоточные эксперименты

### Уровень шума

- **0.005**: Очень чистые данные
- **0.01**: Стандартные условия
- **0.02**: Реалистичные условия (рекомендуется)

### Временные параметры

- **10-15 точек**: Быстрые тесты
- **20-30 точек**: Стандартные исследования
- **50+ точек**: Детальная временная динамика

## 📚 Документация

Подробная документация доступна в:
- [docs/EEG_DATA_GENERATION.md](docs/EEG_DATA_GENERATION.md) - Полное руководство
- [examples/eeg_realistic_demo.ipynb](examples/eeg_realistic_demo.ipynb) - Интерактивный пример
- [examples/eeg_data_demo.py](examples/eeg_data_demo.py) - Командная строка

## 🔄 Миграция с простых данных

Если у вас есть существующий код с простыми данными:

```python
# Старый код
simple_data = [[0, 0, 0, 0, 1.0], [1, 1, 1, 1, 0.5]]
solver = EEGInverseSolver(measured_points=simple_data)

# Новый код
measured_points, generator = create_eeg_experiment()
solver = EEGInverseSolver()
results = solver.solve(measured_points)
```

## 🛠️ Разработчикам

### Модульная архитектура

```
src/inverse_npde/
├── eeg_data_generator.py    # Генерация данных
├── julia_interface.py       # Интерфейс с решателем
└── ...

examples/
├── eeg_realistic_demo.ipynb    # Полный пример
├── eeg_data_demo.py            # Командная строка
└── quick_start_demo.ipynb      # Быстрый старт
```

### Расширение функционала

```python
# Добавление новых источников
generator.add_source(
    position=[x, y, z],
    amplitude=A,
    peak_time=t_peak,
    sigma_time=sigma
)

# Кастомная временная динамика (наследование)
class CustomSource(EEGSignalSource):
    def signal_at_point(self, t, point):
        # Ваша логика
        return custom_signal
```

## 🎉 Заключение

Новые возможности позволяют:

✅ **Создавать реалистичные данные** максимально приближенные к реальным EEG измерениям

✅ **Валидировать алгоритмы** с известными источниками для проверки точности

✅ **Экспериментировать** с различными конфигурациями датчиков и источников

✅ **Визуализировать результаты** с профессиональными графиками

✅ **Интегрировать легко** с существующим кодом решателя

Начните с базового примера и постепенно изучайте более сложные возможности!

---

**💡 Совет**: Начните с `python examples/eeg_data_demo.py basic` для быстрого знакомства с возможностями.