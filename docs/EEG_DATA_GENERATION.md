# Генерация реалистичных EEG данных для обратной задачи

## 🆕 Последние обновления

### Ограничение на верхнюю часть головы (важное изменение!)

**КРИТИЧНО**: Система теперь генерирует датчики **только на верхней части головы** (Z ≥ 0), что точно соответствует реальным EEG системам.

- **Раньше**: Датчики располагались по всей поверхности сферы (Z от -1 до 1)
- **Сейчас**: Датчики только на верхней части (Z от 0 до 1)
- **Преимущества**: Соответствует реальным EEG шапочкам, более реалистичное моделирование

### Улучшенная визуализация

- **3D сетка головы**: Показывает эллипсоидальную форму головы
- **Нумерация датчиков**: Каждый датчик пронумерован для легкой идентификации
- **Множественные проекции**: 
  - **Вид сверху**: проекция на плоскость XY
  - **Вид спереди**: проекция на плоскость XZ  
  - **Боковой вид**: проекция на плоскость YZ
- **Контуры головы**: Показывают форму головы в 2D проекциях
- **Информационные панели**: Отображают размеры головы и конфигурацию

## Обзор

Данный модуль предоставляет возможности для создания реалистичных EEG данных для тестирования и демонстрации алгоритмов решения обратных задач электромагнитных уравнений.

## Основные компоненты

### 1. EEGSensorGenerator

Класс для генерации координат датчиков на эллипсоидальной поверхности головы, имитирующей реальную EEG шапочку.

**Особенности:**
- **Только верхняя часть**: Датчики располагаются только на верхней части головы (Z ≥ 0)
- **Эллипсоидальная форма**: 5 типов головы с реалистичными пропорциями
- **Асимметрия**: Учитывает анатомические особенности (затылок выступает, левая сторона меньше)
- **Улучшенная визуализация**: 3D сетка, нумерация датчиков, множественные проекции

**Пример использования:**
```python
from inverse_npde.eeg_data_generator import EEGSensorGenerator

# Создаем 64 датчика на голове взрослого типа
sensor_gen = EEGSensorGenerator(head_type="adult", num_sensors=64)
positions = sensor_gen.get_sensor_positions()

# Проверяем, что все Z ≥ 0
import numpy as np
assert np.all(positions[:, 2] >= 0), "Все датчики должны быть на верхней части!"

# Визуализируем с улучшенным отображением
sensor_gen.plot_sensors("eeg_sensors_enhanced.png")
```

### 2. EEGSignalSource

Класс для моделирования источников сигнала с временными пиками.

**Особенности:**
- Гауссовские временные пики
- Пространственное затухание (подобное электромагнитному полю)
- Настраиваемая амплитуда и временные характеристики

**Пример использования:**
```python
from inverse_npde.eeg_data_generator import EEGSignalSource

# Создаем источник в позиции (-3, 2, 1)
source = EEGSignalSource(
    position=[-3.0, 2.0, 1.0],
    amplitude=1.0,
    peak_time=0.3,
    sigma_time=0.08
)

# Вычисляем сигнал в точке
signal_value = source.signal_at_point(t=0.5, point=[0.0, 0.0, 0.0])
```

### 3. EEGDataGenerator

Основной класс для генерации измеренных данных.

**Возможности:**
- Множественные источники сигнала
- Настраиваемое количество датчиков и временных точек
- Добавление шума
- Комплексная визуализация
- Сохранение данных в JSON

**Пример использования:**
```python
from inverse_npde.eeg_data_generator import EEGDataGenerator

# Создаем генератор
generator = EEGDataGenerator(num_sensors=64, num_time_points=20, noise_level=0.01)

# Добавляем источники
generator.add_source(position=[-3.0, 2.0, 1.0], amplitude=1.0)
generator.add_source(position=[3.0, -1.0, 2.0], amplitude=0.8)

# Генерируем данные
measured_points = generator.generate_measured_points()

# Визуализируем
generator.visualize_data(measured_points, "results")
```

## Быстрый старт

### Функция create_eeg_experiment

Создает полный EEG эксперимент с разумными параметрами по умолчанию:

```python
from inverse_npde.eeg_data_generator import create_eeg_experiment

# Создаем эксперимент с параметрами по умолчанию
measured_points, generator = create_eeg_experiment(
    num_sensors=32,        # 32 датчика
    num_time_points=15,    # 15 временных точек
    noise_level=0.01,      # 1% шума
    save_visualization=True # Создаем графики
)

print(f"Сгенерировано {len(measured_points)} измеренных точек")
```

### Использование с решателем

После генерации данных их можно использовать с решателем обратной задачи:

```python
from inverse_npde.julia_interface import EEGInverseSolver

# Создаем решатель
solver = EEGInverseSolver(
    neural_config={
        'hidden_layers': [64, 32],
        'activation': 'tanh',
    }
)

# Решаем обратную задачу
results = solver.solve(measured_points)
```

## Параметры конфигурации

### EEGSensorGenerator

| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|----------------------|----------|
| radius | float | 10.0 | Радиус сферы (размер головы) |
| num_sensors | int | 64 | Количество датчиков |

### EEGSignalSource

| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|----------------------|----------|
| position | List[float] | - | [x, y, z] координаты источника |
| amplitude | float | 1.0 | Амплитуда сигнала |
| peak_time | float | 0.5 | Время пика сигнала |
| sigma_time | float | 0.1 | Ширина пика (стандартное отклонение) |

### EEGDataGenerator

| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|----------------------|----------|
| num_sensors | int | 64 | Количество датчиков |
| num_time_points | int | 10 | Количество временных точек |
| noise_level | float | 0.01 | Уровень шума (стандартное отклонение) |

## Структура данных

### Измеренные точки

Каждая измеренная точка представляет собой список из 5 элементов:
```python
[x, y, z, t, measurement]
```

Где:
- `x, y, z` - координаты датчика
- `t` - время измерения
- `measurement` - значение сигнала в этой точке

### Пример данных

```python
measured_points = [
    [5.23, -2.18, 8.91, 0.0, 0.012],   # Точка 1
    [5.23, -2.18, 8.91, 0.07, 0.045],  # Точка 2 (тот же датчик, другое время)
    # ...
]
```

## Визуализация

### Автоматическая визуализация

При вызове `visualize_data()` создаются следующие графики:

1. **EEG датчики (3D + 2D проекция)**
   - Трехмерная визуализация датчиков на сфере
   - Двухмерная проекция сверху

2. **Временные ряды**
   - Графики сигнала для нескольких датчиков
   - Показывает временную динамику сигналов

3. **Пространственное распределение**
   - 6 панелей с пространственным распределением сигнала в разные моменты времени
   - Цветовое кодирование интенсивности сигнала

### Сохранение данных

```python
# Сохранение в JSON файл
generator.save_data(measured_points, "my_eeg_data.json")

# Файл содержит:
# - Конфигурацию эксперимента
# - Позиции датчиков
# - Параметры источников
# - Все измеренные точки
```

## Практические рекомендации

### Выбор количества датчиков

- **16-32 датчика**: Быстрые эксперименты, демонстрация
- **64 датчика**: Стандартные исследования
- **128+ датчика**: Высокоточные эксперименты

### Уровень шума

- **0.005**: Очень чистые данные
- **0.01**: Стандартные условия
- **0.02**: Шумные условия (более реалистично)

### Количество временных точек

- **10-15**: Быстрые тесты
- **20-30**: Стандартные исследования
- **50+**: Детальная временная динамика

## Интеграция с примерами

### В ноутбуке `eeg_realistic_demo.ipynb`

Полнофункциональный пример, включающий:
- Генерацию данных
- Решение обратной задачи
- Визуализацию результатов
- Сравнение с аналитическим решением
- Анализ качества восстановления

### В примере `examples/eeg_data_demo.py`

Скрипт для командной строки:
```bash
python examples/eeg_data_demo.py
```

## Расширение функционала

### Добавление новых источников

```python
# Добавляем источник с кастомными параметрами
generator.add_source(
    position=[5.0, -3.0, 0.0],
    amplitude=1.5,           # Выше амплитуда
    peak_time=0.8,          # Поздний пик
    sigma_time=0.05         # Узкий пик
)
```

### Кастомная временная динамика

```python
# Для создания нестандартных временных профилей
# можно наследовать от EEGSignalSource и переопределить метод signal_at_point()
```

## Отладка и диагностика

### Проверка качества данных

```python
# Анализируем статистику данных
import numpy as np

data = np.array(measured_points)
signals = data[:, 4]  # Только сигналы

print(f"Средний сигнал: {np.mean(signals):.6f}")
print(f"Стандартное отклонение: {np.std(signals):.6f}")
print(f"Минимум: {np.min(signals):.6f}")
print(f"Максимум: {np.max(signals):.6f}")
```

### Визуализация источников

```python
# Показываем все источники на 3D графике
fig = plt.figure(figsize=(10, 8))
ax = fig.add_subplot(111, projection='3d')

# Датчики
ax.scatter(generator.sensor_gen.sensor_positions[:, 0], 
          generator.sensor_gen.sensor_positions[:, 1], 
          generator.sensor_gen.sensor_positions[:, 2], 
          c='red', s=50, alpha=0.6, label='Датчики')

# Источники
for i, source in enumerate(generator.sources):
    pos = source.position
    ax.scatter(pos[0], pos[1], pos[2], c='blue', s=200, 
              marker='^', alpha=0.8, label=f'Источник {i+1}')

ax.set_xlabel('X')
ax.set_ylabel('Y')
ax.set_zlabel('Z')
ax.legend()
plt.show()
```

## Заключение

Модуль генерации EEG данных предоставляет мощные и гибкие возможности для создания реалистичных тестовых данных. Он особенно полезен для:

- Тестирования алгоритмов обратных задач
- Демонстрации возможностей системы
- Обучения и исследований
- Валидации новых методов

Благодаря модульной архитектуре и богатым возможностям настройки, модуль подходит как для простых демонстраций, так и для серьезных исследований в области обратных задач электромагнитных уравнений.